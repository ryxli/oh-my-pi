/**
 * `@oh-my-pi/pi-dstui-tui` public API.
 *
 * Thin adapter between `@oh-my-pi/pi-dstui` and `@oh-my-pi/pi-tui`:
 * exposes a focusable `DstuiComponent` wrapper plus an
 * `mountDstuiOverlay` helper for the agent extension surface.
 */

export * from "./bundled";
export * from "./component";
export * from "./overlay";
